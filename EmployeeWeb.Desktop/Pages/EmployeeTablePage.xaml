<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="EmployeeWeb.Desktop.Pages.EmployeeTablePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Employee Table" FontSize="20" FontWeight="SemiBold"/>
                <TextBlock x:Name="SubtitleText"
                           Text="Daily summary of all employees."
                           Margin="0,4,0,0"
                           Foreground="#666"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom" Spacing="8">
                <TextBlock Text="Date:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                <DatePicker x:Name="DatePicker"
                            SelectedDateChanged="DatePicker_SelectedDateChanged"
                            Width="160"/>
            </StackPanel>
        </Grid>

        <!-- Table -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Column headers -->
                <Grid Grid.Row="0" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Name" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="1" Text="Staff ID" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="2" Text="First Login" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="3" Text="Last Logout" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="4" Text="Total Work" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="5" Text="Total Break" FontWeight="SemiBold"/>
                </Grid>

                <!-- Rows -->
                <ListView x:Name="EmployeeTable"
                          Grid.Row="1"
                          IsItemClickEnabled="False">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="{Binding StaffName}" />
                                <TextBlock Grid.Column="1" Text="{Binding StaffID}" />
                                <TextBlock Grid.Column="2" Text="{Binding FirstLogin}" />
                                <TextBlock Grid.Column="3" Text="{Binding LastLogout}" />
                                <TextBlock Grid.Column="4" Text="{Binding TotalWork}" />
                                <TextBlock Grid.Column="5" Text="{Binding TotalBreak}" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</Page>
