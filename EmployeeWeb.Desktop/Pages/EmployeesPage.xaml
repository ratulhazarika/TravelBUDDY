<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="EmployeeWeb.Desktop.Pages.EmployeesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header row -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Employees" FontSize="20" FontWeight="SemiBold"/>
                <TextBlock x:Name="SubtitleText"
                           Text="Select an employee from the search bar."
                           Margin="0,4,0,0"
                           Foreground="#666"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom" Spacing="8">
                <TextBlock Text="Date:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                <DatePicker x:Name="DatePicker"
                            SelectedDateChanged="DatePicker_SelectedDateChanged"
                            Width="160"/>
            </StackPanel>
        </Grid>

        <!-- Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: profile card -->
            <Border Grid.Column="0"
                    Background="White"
                    CornerRadius="8"
                    Padding="16"
                    Margin="0,0,16,0">
                <StackPanel Spacing="8">
                    <TextBlock Text="Profile" FontWeight="SemiBold" FontSize="16"/>
                    <Rectangle Height="1" Fill="#EEEEEE" Margin="0,4,0,8"/>

                    <TextBlock x:Name="ProfileNameText"
                               Text="No employee selected"
                               FontSize="18"
                               FontWeight="SemiBold"/>
                    <TextBlock x:Name="ProfileRoleText"
                               Text=""
                               Foreground="#666"/>

                    <StackPanel Margin="0,12,0,0" Spacing="4">
                        <TextBlock Text="Staff ID" FontWeight="SemiBold" FontSize="12" Foreground="#777"/>
                        <TextBlock x:Name="ProfileStaffIdText" Text="-" />
                    </StackPanel>

                    <StackPanel Margin="0,8,0,0" Spacing="4">
                        <TextBlock Text="Email" FontWeight="SemiBold" FontSize="12" Foreground="#777"/>
                        <TextBlock x:Name="ProfileEmailText" Text="-" TextWrapping="Wrap"/>
                    </StackPanel>

                    <StackPanel Margin="0,8,0,0" Spacing="4">
                        <TextBlock Text="Phone" FontWeight="SemiBold" FontSize="12" Foreground="#777"/>
                        <TextBlock x:Name="ProfilePhoneText" Text="-" />
                    </StackPanel>

                    <StackPanel Margin="0,8,0,0" Spacing="4">
                        <TextBlock Text="Status" FontWeight="SemiBold" FontSize="12" Foreground="#777"/>
                        <TextBlock x:Name="ProfileStatusText" Text="-" />
                    </StackPanel>

                    <StackPanel Margin="0,8,0,0" Spacing="4">
                        <TextBlock Text="Type" FontWeight="SemiBold" FontSize="12" Foreground="#777"/>
                        <TextBlock x:Name="ProfileTypeText" Text="-" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Right: activity snapshot + time log -->
            <StackPanel Grid.Column="1" Spacing="16">
                <Border Background="White" CornerRadius="8" Padding="16">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Daily Activity Snapshot" FontWeight="SemiBold" FontSize="16"/>
                        <Rectangle Height="1" Fill="#EEEEEE" Margin="0,4,0,8"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Total Work" FontSize="12" Foreground="#777"/>
                                <TextBlock x:Name="TotalWorkText" FontSize="16" FontWeight="SemiBold" Text="0h 0m"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Total Break" FontSize="12" Foreground="#777"/>
                                <TextBlock x:Name="TotalBreakText" FontSize="16" FontWeight="SemiBold" Text="0h 0m"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Status" FontSize="12" Foreground="#777"/>
                                <TextBlock x:Name="StatusText" FontSize="16" FontWeight="SemiBold" Text="N/A"/>
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,12,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="First Login" FontSize="12" Foreground="#777"/>
                                <TextBlock x:Name="FirstLoginText" FontSize="14" Text="N/A"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Last Logout" FontSize="12" Foreground="#777"/>
                                <TextBlock x:Name="LastLogoutText" FontSize="14" Text="N/A"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Background="White" CornerRadius="8" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Time Log" FontWeight="SemiBold" FontSize="16"/>
                        <Rectangle Height="1" Fill="#EEEEEE" Margin="0,4,0,8"/>
                        <ListView x:Name="TimeLogList" Height="260">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="140"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding TimeFormatted}"
                                                   Grid.Column="0"
                                                   Foreground="#555"/>
                                        <TextBlock Text="{Binding Label}"
                                                   Grid.Column="1"
                                                   Foreground="#333"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
